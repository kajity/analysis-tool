[project]
name = "analysis-tool"
version = "0.1.0"
description = "Reproducible Python analysis workflows for experimental data."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.0",
    "numpy>=2.3.0",
    "omegaconf>=2.3.0",
    "scipy>=1.17.1",
]

[tool.uv]
package = false
